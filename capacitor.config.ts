import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sealevel.visualizer',
  appName: 'Sea Level Visualizer',
  webDir: 'dist'
};

export default config;
